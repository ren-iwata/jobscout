現在の状態を PROGRESS.md に保存せよ（完了／未完了／次の一手／申し送り）。docs/08_worklog.md の末尾に本セッションの日誌を追記せよ（形式:「## 日付｜タスク名」の下に、やったこと／詰まった点と解決／申し送り を合計3〜5行）。docs/06_open_questions.md に未回答があれば選択肢付きで一括提示せよ。_handoff_to_hq/ に current_status ファイル（命名は `<案件名>_current_status.md`）が存在するプロジェクトでは、遅い状態（フェーズ／マイルストーン残距離／CEO判断待ち／リスク）に同ファイルからの変化があった場合のみ、同ファイルを更新（冒頭「時点:」を当日に）した上で、末尾に「■CEO作業: 事業部プロジェクトの current_status を差し替え」として新しい current_status 全文を1コードブロックで出力せよ（変化がなければ何も出さない）。使用中のモデルが既定（Sonnet 5）以外なら「/model で Sonnet 5 に戻してください」と案内せよ。最後に「/clear を実行し、次回は /next と入力してください」とだけ出力して終了。
